# predicted_masks

Carpeta de salida **exigida por el enunciado**. Al correr el pipeline
(`pipeline.run()` o el notebook), aquí se guardan las máscaras segmentadas
(`01_h.png`, `01_dr.png`, ...), una por cada imagen del dataset.

No edites estos archivos a mano: se regeneran en cada corrida.
